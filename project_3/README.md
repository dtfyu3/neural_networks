# Plant Species Classification using Transfer Learning
## (Классификация видов растений с использованием переноса обучения)

### 📌 Описание проекта
Данный проект решает задачу **мультиклассовой классификации изображений** для автоматического распознавания видов растений (листьев или ростков). 
Используется подход **Transfer Learning**: модель, предварительно обученная на миллионах изображений (ImageNet), дообучается (Fine-tuning) на специфическом наборе данных растений.

### 🎯 Цель
Разработать устойчивую модель компьютерного зрения, способную различать 10+ сортов растений в условиях естественной съемки (разный фон, освещение, поворот), и предоставить детальный отчет о качестве распознавания каждого класса.

### 📊 Данные
*   **Источник:** [Leafsnap Dataset](https://www.kaggle.com/datasets/xhlulu/leafsnap-dataset/data).
*   **Структура:** Изображения рассортированы по папкам, где имя папки соответствует классу растения.
*   **Классы:** 16 видов (включая *Acer rubrum*, *Quercus bicolor*, *Abies concolor* и др.).
*   **Предобработка:** 
    *   Удаление системных файлов (.ipynb_checkpoints).
    *   Автоматическое разбиение на Train (80%) и Validation (20%).

### 🧠 Архитектура и Методы
1.  **Модель:** **ResNet18**.
    *   Легковесная сверточная нейросеть (CNN), оптимальная для задач с ограниченным объемом данных.
    *   Базовые слои "заморожены" (Frozen), обучается только выходной полносвязный слой (Head).
2.  **Аугментация данных (Data Augmentation):**
    *   Используются случайные повороты (`RandomRotation`), отражения (`HorizontalFlip`) и изменения яркости/контраста (`ColorJitter`) для предотвращения переобучения.
3.  **Оптимизация:**
    *   Loss Function: CrossEntropyLoss.
    *   Optimizer: Adam (learning rate = 0.001).
4.  **Checkpointing:**
    *   Система сохраняет веса модели после обучения. При повторном запуске скрипт автоматически загружает обученную модель, пропуская этап тренировки.

### 📉 Метрики и Результаты
Для оценки качества используются:
*   **Per-class Accuracy:** Точность, полнота (Recall) и F1-score для каждого отдельного сорта.
*   **Confusion Matrix:** Визуализация ошибок (какие сорта модель путает между собой).

*Итоговая точность на валидации: 94%*

### 🛠 Инструкция по запуску

1.  **Установка зависимостей:**
    ```bash
    pip install -r requirements.txt
    ```

2.  **Структура папок:**
    Поместите изображения в папку `data/`, чтобы структура была такой:
    ```text
    data/
      ├── Сорт_1/
      │    ├── image1.jpg
      │    └── ...
      ├── Сорт_2/
      └── ...
    ```

3.  **Запуск скрипта:**
    ```bash
    python train.py
    ```
    *Результаты (графики и веса модели) появятся в той же папке.*